import React from "react";

export default function Default() {
  return (
    <div
      style={{
        fontWeight: "bold",
        fontSize: "50px",
        display: "flex",
        justifyContent: "center",
      }}
    >
      Page Not Found
    </div>
  );
}
